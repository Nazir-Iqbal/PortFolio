[project]
name = "portfolio"
version = "1.0.0"
description = "Nazir Iqbal - Portfolio Website"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = []
