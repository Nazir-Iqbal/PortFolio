[project]
name = "portfolio"
version = "1.0.0"
description = "Nazir Iqbal - Portfolio Website"
requires-python = ">=3.9"
dependencies = [
    "django>=4.2,<4.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
